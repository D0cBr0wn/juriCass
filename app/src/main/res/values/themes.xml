<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.JuriCass" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/status_bar</item>
    </style>

    <style name="Calandar" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">@color/white</item>
        <item name="android:calendarTextColor">@color/white</item>
        <item name="android:focusedMonthDateColor">@color/success</item>
    </style>
</resources>